Front-end scaffold placeholder.

This project will use React + TypeScript + Vite. To create the app:

```bash
pnpm create vite@latest frontend -- --template react-ts
cd frontend
pnpm install
pnpm dev
```

The frontend will call the backend API at the URL specified by an environment variable (e.g., VITE_API_URL).
